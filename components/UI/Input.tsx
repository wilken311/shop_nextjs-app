import React, {ReactElement} from "react";

interface IInputProps {
}

const Input: React.FunctionComponent<IInputProps> = (props): ReactElement => {
  return (
    <></>
  );
};

export default Input;